# reports/

Space for written business-insight summaries (e.g. a one-page PDF/Markdown
writeup of the findings in `data/processed/statistical_test_results.csv`
and `shap_feature_importance.csv`, aimed at a non-technical stakeholder).
Nothing here yet — the notebooks in `python/` are the primary output of this
project.

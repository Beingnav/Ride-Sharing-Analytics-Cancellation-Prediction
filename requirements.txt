pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44
scipy>=1.10
statsmodels>=0.14
matplotlib>=3.7
seaborn>=0.12
plotly>=5.18
sqlalchemy>=2.0
pymysql>=1.1
joblib>=1.3
faker>=22.0
jupyter>=1.0
nbformat>=5.9
streamlit>=1.32

# macOS only: XGBoost needs the OpenMP runtime, which its PyPI wheel does not bundle.
# If `import xgboost` fails with "Library not loaded: @rpath/libomp.dylib", run:
#   brew install libomp
# (or use conda: `conda install -c conda-forge py-xgboost`)
